#include <iostream>
using namespace std;

int main() {
    /* STEP 0. */
    const int ROCK = 0;
    const int PAPER = 1;
    const int SCISSORS = 2;

    int seed;
    cin >> seed;
    srand(seed);

    /* Type your code here. */
    /* STEP 1. */


    return 0;
}